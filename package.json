{
  "name": "track-time",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@types/bun": "latest",
    "ultracite": "6.0.5",
    "tailwindcss": "^4.1.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --bun src/index.ts",
    "build": "bun --bun build.ts",
    "start": "cd dist && NODE_ENV=production bun --bun index.js",
    "check": "bunx --bun ultracite check",
    "fix": "bun --bun ultracite fix"
  },
  "dependencies": {
    "bun-plugin-tailwind": "^0.1.2"
  }
}
