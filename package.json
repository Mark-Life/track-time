{
  "name": "track-time",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "tailwindcss": "latest",
    "ultracite": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --bun src/index.ts",
    "build": "bun --bun build.ts",
    "start": "cd dist && NODE_ENV=production bun --bun --env-file=../.env index.js",
    "check": "bunx --bun ultracite check",
    "fix": "bun --bun ultracite fix"
  },
  "dependencies": {
    "bun-plugin-tailwind": "latest",
    "effect": "latest"
  }
}
