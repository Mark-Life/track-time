{
  "name": "track-time",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@effect/language-service": "^0.55.2",
    "@types/bun": "latest",
    "tailwindcss": "latest",
    "ultracite": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --bun src/index.ts",
    "build": "bun run --bun build.ts",
    "start": "cd dist && NODE_ENV=production bun run --bun --env-file=../.env index.js",
    "check": "bunx run --bun ultracite check",
    "fix": "bun run --bun ultracite fix"
  },
  "dependencies": {
    "bun-plugin-tailwind": "latest",
    "effect": "latest"
  }
}
